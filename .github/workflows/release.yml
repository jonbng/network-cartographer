name: Release CLI

on:
  push:
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-22.04
            artifact: linux-x86_64
            archive: network-cartographer-linux-x86_64.tar.gz
          - os: ubuntu-24.04-arm
            artifact: linux-arm64
            archive: network-cartographer-linux-arm64.tar.gz
          - os: macos-15
            artifact: macos-arm64
            archive: network-cartographer-macos-arm64.tar.gz
          - os: macos-15-intel
            artifact: macos-x86_64
            archive: network-cartographer-macos-x86_64.tar.gz
          - os: windows-latest
            artifact: windows-x86_64
            archive: network-cartographer-windows-x86_64.zip
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: server
          key: release-${{ matrix.artifact }}
      - run: npm ci
      - run: npm run build
      - run: cargo build --release --locked --manifest-path server/Cargo.toml
      - name: Smoke test CLI (Unix)
        if: runner.os != 'Windows'
        run: server/target/release/netcart --help
      - name: Smoke test CLI (Windows)
        if: runner.os == 'Windows'
        run: server/target/release/netcart.exe --help
      - name: Archive CLI (Unix)
        if: runner.os != 'Windows'
        run: |
          mkdir "network-cartographer-${{ matrix.artifact }}"
          cp "server/target/release/netcart" "network-cartographer-${{ matrix.artifact }}/"
          tar -czf "network-cartographer-${{ matrix.artifact }}.tar.gz" "network-cartographer-${{ matrix.artifact }}"
      - name: Archive CLI (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          New-Item -ItemType Directory "network-cartographer-${{ matrix.artifact }}"
          Copy-Item "server/target/release/netcart.exe" "network-cartographer-${{ matrix.artifact }}/"
          Compress-Archive "network-cartographer-${{ matrix.artifact }}" "network-cartographer-${{ matrix.artifact }}.zip"
      - uses: actions/upload-artifact@v4
        with:
          name: network-cartographer-${{ matrix.artifact }}
          path: ${{ matrix.archive }}
          if-no-files-found: error

  release:
    if: startsWith(github.ref, 'refs/tags/')
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          pattern: network-cartographer-*
          path: release-assets
          merge-multiple: true
      - name: Generate checksums
        working-directory: release-assets
        run: sha256sum network-cartographer-* > SHA256SUMS
      - uses: softprops/action-gh-release@v2
        with:
          draft: false
          generate_release_notes: true
          body: |
            ## Run it

            macOS or Linux:

            ```bash
            curl -fsSL https://mapmy.network/run | sh
            ```

            Windows PowerShell:

            ```powershell
            irm https://mapmy.network/run.ps1 | iex
            ```

            This starts Network Cartographer as a local CLI and opens the browser UI. Stop it with Ctrl+C; the process exits and cleans itself up.
          files: |
            release-assets/*
            scripts/run.sh
            scripts/run.ps1
