# Server-only. Never prefix these with NEXT_PUBLIC_.
# Point at the private VPS geo service (POST /v1/lookup).
GEO_PROVIDER_URL=http://127.0.0.1:8787/v1/lookup
GEO_PROVIDER_TOKEN=

# Upstash Redis REST credentials for the anonymous run counter.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
